# 🚀 Subir a GitHub - Instrucciones Rápidas

## Nombre del Repositorio: `cursor-termux-bridge`

### Método Rápido (Recomendado)

1. **Crea el repositorio en GitHub:**
   - Ve a: https://github.com/new
   - Nombre: `cursor-termux-bridge`
   - Descripción: `Bridge system to code and query Cursor/IA from Termux on Android`
   - Elige: **Público** o **Privado**
   - ⚠️ **IMPORTANTE**: NO marques "Add a README file" (ya tenemos uno)
   - Click en **"Create repository"**

2. **Copia la URL que GitHub te muestra** (algo como: `https://github.com/TU_USUARIO/cursor-termux-bridge.git`)

3. **Ejecuta estos comandos en tu terminal:**

```bash
git remote add origin https://github.com/TU_USUARIO/cursor-termux-bridge.git
git push -u origin main
```

**Reemplaza `TU_USUARIO` con tu nombre de usuario de GitHub.**

### Verificación

Después de subir, deberías poder ver el repositorio en:
`https://github.com/TU_USUARIO/cursor-termux-bridge`

### Clonar en Termux

Una vez subido, puedes clonarlo en Termux:

```bash
cd ~
git clone https://github.com/TU_USUARIO/cursor-termux-bridge.git
cd cursor-termux-bridge
```

¡Listo! 🎉
